import { DefaultAccessibilityDirective } from './default-accessibility.directive';

describe('DefaultAccessibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultAccessibilityDirective();
    expect(directive).toBeTruthy();
  });
});
