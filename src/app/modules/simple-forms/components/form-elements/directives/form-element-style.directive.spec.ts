import { FormElementStyleDirective } from './form-element-style.directive';

describe('FormElementStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new FormElementStyleDirective();
    expect(directive).toBeTruthy();
  });
});
