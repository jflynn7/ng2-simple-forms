import { FormElementValidationDirective } from './form-element-validation.directive';

describe('FormElementValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new FormElementValidationDirective();
    expect(directive).toBeTruthy();
  });
});
