import { LabelAccessibilityDirective } from './label-accessibility.directive';

describe('LabelAccessibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new LabelAccessibilityDirective();
    expect(directive).toBeTruthy();
  });
});
